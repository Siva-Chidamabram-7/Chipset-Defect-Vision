# ── DEPRECATED — use !training/data.yaml ──────────────────────────────────────
# This file is kept for backwards compatibility only.
# All training runs must use:  --data !training/data.yaml
#
# This file will not be updated further and will be removed in a future cleanup.
# ─────────────────────────────────────────────────────────────────────────────

path: ../data

train: images/train
val:   images/val

nc: 6
names:
  0: Missing_hole
  1: Mouse_bite
  2: Open_circuit
  3: Short
  4: Spur
  5: Spurious_copper
