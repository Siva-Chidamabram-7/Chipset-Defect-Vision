# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── Computer Vision ───────────────────────────────────────────────────────────
ultralytics==8.2.18        # YOLOv8
opencv-python-headless==4.9.0.80
Pillow==10.3.0
numpy==1.26.4

# ── PyTorch (CPU wheel — swap for GPU in Dockerfile if needed) ────────────────
# Installed automatically by ultralytics; pin here for reproducibility.
# torch>=2.0.0            # uncomment to force a specific torch build
# torchvision>=0.15.0

# ── Utilities ─────────────────────────────────────────────────────────────────
pydantic==2.7.1
PyYAML==6.0.1
requests==2.31.0
tqdm==4.66.2
