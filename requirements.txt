# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── Machine Learning ──────────────────────────────────────────────────────────
# torch + torchvision are intentionally NOT listed here.
# They are installed from the official CPU-only wheel in the Dockerfile to avoid
# pulling the 2 GB GPU bundle from PyPI.
# For local dev:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
ultralytics==8.2.18

# ── Computer Vision ───────────────────────────────────────────────────────────
# opencv-python-headless replaces opencv-python (no libGL dependency).
# The Dockerfile installs this AFTER ultralytics to prevent dual-cv2 conflict.
# Do NOT pin opencv here — let the Dockerfile control the swap.
numpy==1.26.4
Pillow==10.3.0

# ── Utilities ─────────────────────────────────────────────────────────────────
pydantic==2.7.1
PyYAML==6.0.1

# ── Cloud Storage (optional — needed only if inference server reads from GCS) ─
# Remove these two lines if you are running fully offline / air-gapped.
gcsfs==2024.3.1
google-cloud-storage==2.16.0
